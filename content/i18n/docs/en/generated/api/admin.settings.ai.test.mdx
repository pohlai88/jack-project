---
title: 'Admin Settings Ai Test'
description: 'Validate tenant AI provider settings.'
docsType: 'generated-evidence'
---

import { Banner } from 'fumadocs-ui/components/banner';
import { Callout } from 'fumadocs-ui/components/callout';
import { File, Files, Folder } from 'fumadocs-ui/components/files';
import { Step, Steps } from 'fumadocs-ui/components/steps';
import { Tab, Tabs } from 'fumadocs-ui/components/tabs';

{/_
GENERATED FILE - DO NOT EDIT
Source: docs inventory graph
Generated by: pnpm docs:generate
_/}

<Banner id="api-admin.settings.ai.test" changeLayout={false}>
  GENERATED EVIDENCE - This page is rendered from validated product truth.
</Banner>

<Callout title="Truth Surface: admin.settings.ai.test" type="info">
  - API ID: `admin.settings.ai.test` - Route: `POST /api/tenants/[tenant]/admin/settings/ai/test` - Owning feature:
  `admin` - OpenAPI reference: `/docs/openapi`
</Callout>

## Overview: what this API actually does

Validate tenant AI provider settings.

## When to use this evidence

Use this page before changing public contracts, route guards, or payload semantics.

## Source contract

<TypeTable
  type={{
    API: {
      type: 'admin.settings.ai.test',
      description: 'Stable API evidence ID.',
      required: true,
    },
    Method: {
      type: 'POST',
      description: 'HTTP method declared in the feature manifest.',
      required: true,
    },
    Route: {
      type: '/api/tenants/[tenant]/admin/settings/ai/test',
      description: 'Next.js route covered by this API evidence.',
      required: true,
    },
    Feature: {
      type: 'admin',
      description: 'Owning feature manifest.',
      required: true,
    },
  }}
/>

## Runtime surfaces: route and OpenAPI

This generated API page is manifest evidence. Where the route is part of the public HTTP surface, keep the
OpenAPI schema aligned so Fumadocs can render request and response details from the machine-readable contract.

See [OpenAPI Reference](/docs/openapi) for documented request and response interface truth.

<Files>
  <Folder name="API Route" defaultOpen>
    <File name="POST /api/tenants/[tenant]/admin/settings/ai/test" />
  </Folder>
</Files>

## Failure modes

Failure behavior is owned by the feature manifest and the runtime route. Confirm status codes and response semantics in the OpenAPI reference before exposing the route externally.

## Source evidence: traceability

<Steps>
  <Step>Manifest: `src/features/admin/docs.manifest.ts`.</Step>
  <Step>Confirm the Next.js route `/api/tenants/[tenant]/admin/settings/ai/test` exists.</Step>
  <Step>Update OpenAPI coverage for public or operator-facing APIs.</Step>
</Steps>
