---
title: 'Dashboard'
description: 'Landing and tenant dashboard surfaces for workspace entry, activity, stats, and quick actions.'
docsType: 'generated-evidence'
---

import { Banner } from 'fumadocs-ui/components/banner';
import { Callout } from 'fumadocs-ui/components/callout';
import { File, Files, Folder } from 'fumadocs-ui/components/files';
import { Step, Steps } from 'fumadocs-ui/components/steps';
import { Tab, Tabs } from 'fumadocs-ui/components/tabs';

{/_
GENERATED FILE - DO NOT EDIT
Source: docs inventory graph
Generated by: pnpm docs:generate
_/}

<Banner id="feature-dashboard" changeLayout={false}>
  GENERATED EVIDENCE - This page is rendered from validated product truth. Release state: released.
</Banner>

<Callout title="Truth Surface: Dashboard" type="info">
  - Feature ID: `dashboard` - Module: `Workspace` - Owner: `product-platform` - Release state: `released`
</Callout>

## Overview: what this feature actually does

Landing and tenant dashboard surfaces for workspace entry, activity, stats, and quick actions.

## When to use this evidence

Use this page when you need the governed feature boundary for Dashboard: owner, release state, routes,
permissions, workflows, APIs, and failure surfaces. Curated pages may explain intent, but this generated page
is the route and contract evidence.

## Source contract

<TypeTable
  type={{
    Module: {
      type: 'Workspace',
      description: 'Product module covered by this evidence page.',
      required: true,
    },
    Owner: {
      type: 'product-platform',
      description: 'Accountable owner for the feature manifest.',
      required: true,
    },
    'Release state': {
      type: 'released',
      description: 'Release lifecycle state declared by product truth.',
      required: true,
    },
    Routes: {
      type: '4 routes',
      description: 'See Route coverage below.',
      required: true,
    },
    Permissions: {
      type: '0 permissions',
      description: 'See Permission coverage below.',
      required: true,
    },
  }}
/>

## Runtime surfaces: routes and permissions

<Tabs items={["Routes", "Permissions"]}>
<Tab>

<Files>
  <Folder name="Routes" defaultOpen>
    <File name="/" />
    <File name="/[locale]" />
    <File name="/[locale]/t/[tenant]" />
    <File name="/api/health" />
  </Folder>
</Files>

</Tab>
<Tab>

<Files>
  <Folder name="Permissions" defaultOpen>
    <File name="None declared" />
  </Folder>
</Files>

</Tab>
</Tabs>

## Workflows and actions

<Tabs items={["Workflows", "Actions"]}>
<Tab>

### Workspace entry

- ID: `dashboard.workspace-entry`
- Summary: Users land in the tenant workspace and review activity, stats, and quick actions.

</Tab>
<Tab>

### Review workspace status

- ID: `dashboard.review-workspace`
- Summary: Inspect tenant metrics, profile completion, contextual tips, and recent activity.

</Tab>
</Tabs>

## APIs: interface truth

- [`platform.health`](/docs/generated/api/platform.health) - `GET /api/health` - Expose basic application health status.

See [OpenAPI Reference](/docs/openapi) for documented request and response interface truth.

## Failure modes: operator risk

- `AFD-DASHBOARD-STATS` - Dashboard stats unavailable: Check tenant context and dashboard stats service execution.

## Source evidence: traceability

<Steps>
  <Step>Manifest: `src/features/dashboard/docs.manifest.ts`.</Step>
  <Step>Confirm route, permission, and API references still exist in the product runtime.</Step>
  <Step>Run `pnpm docs:generate` and `pnpm docs:ci` after manifest or route changes.</Step>
</Steps>
