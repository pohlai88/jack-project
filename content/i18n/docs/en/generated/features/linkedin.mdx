---
title: 'LinkedIn OAuth Integration'
description: 'LinkedIn OAuth connection entry points for tenant integration setup.'
docsType: 'generated-evidence'
---

import { Banner } from 'fumadocs-ui/components/banner';
import { Callout } from 'fumadocs-ui/components/callout';
import { File, Files, Folder } from 'fumadocs-ui/components/files';
import { Step, Steps } from 'fumadocs-ui/components/steps';
import { Tab, Tabs } from 'fumadocs-ui/components/tabs';

<div
  hidden
  data-generated-docs-header="GENERATED FILE - DO NOT EDIT"
  data-source="docs inventory graph"
  data-generated-by="pnpm docs:generate"
/>
<Banner id="feature-linkedin" changeLayout={false}>
  GENERATED EVIDENCE - This page is rendered from validated product truth. Release state: released.
</Banner>

<Callout title="Truth Surface: LinkedIn OAuth Integration" type="info">
  - Feature ID: `linkedin` - Module: `Integrations` - Owner: `integrations-platform` - Release state: `released`
</Callout>

## Overview: what this feature actually does

LinkedIn OAuth connection entry points for tenant integration setup.

## When to use this evidence

Use this page when you need the governed feature boundary for LinkedIn OAuth Integration: owner, release state, routes,
permissions, workflows, APIs, and failure surfaces. Curated pages may explain intent, but this generated page
is the route and contract evidence.

## Source contract

<TypeTable
  type={{
    Module: {
      type: 'Integrations',
      description: 'Product module covered by this evidence page.',
      required: true,
    },
    Owner: {
      type: 'integrations-platform',
      description: 'Accountable owner for the feature manifest.',
      required: true,
    },
    'Release state': {
      type: 'released',
      description: 'Release lifecycle state declared by product truth.',
      required: true,
    },
    Routes: {
      type: '2 routes',
      description: 'See Route coverage below.',
      required: true,
    },
    Permissions: {
      type: '1 permissions',
      description: 'See Permission coverage below.',
      required: true,
    },
  }}
/>

## Runtime surfaces: routes and permissions

<Tabs items={["Routes", "Permissions"]}>
<Tab>

<Files>
  <Folder name="Routes" defaultOpen>
    <File name="/api/integrations/linkedin/callback" />
    <File name="/api/integrations/linkedin/connect" />
  </Folder>
</Files>

</Tab>
<Tab>

<Files>
  <Folder name="Permissions" defaultOpen>
    <File name="admin:integrations" />
  </Folder>
</Files>

</Tab>
</Tabs>

## Workflows and actions

<Tabs items={["Workflows", "Actions"]}>
<Tab>

### LinkedIn OAuth connection

- ID: `linkedin.oauth-connect`
- Summary: Start and complete LinkedIn OAuth for tenant integration setup.

</Tab>
<Tab>

### Connect LinkedIn account

- ID: `linkedin.connect`
- Summary: Generate LinkedIn OAuth state and redirect to LinkedIn authorization.

</Tab>
</Tabs>

## APIs: interface truth

- [`linkedin.oauth.callback`](/docs/generated/api/linkedin.oauth.callback) - `GET /api/integrations/linkedin/callback` - Handle LinkedIn OAuth callback.
- [`linkedin.oauth.connect`](/docs/generated/api/linkedin.oauth.connect) - `GET /api/integrations/linkedin/connect` - Start LinkedIn OAuth authorization.

See [OpenAPI Reference](/docs/openapi) for documented request and response interface truth.

## Failure modes: operator risk

- `AFD-LINKEDIN-OAUTH` - LinkedIn OAuth failed: Validate OAuth state, LinkedIn credentials, callback URL, and tenant context.

## Source evidence: traceability

<Steps>
  <Step>Manifest: `src/features/linkedin/docs.manifest.ts`.</Step>
  <Step>Confirm route, permission, and API references still exist in the product runtime.</Step>
  <Step>Run `pnpm docs:generate` and `pnpm docs:ci` after manifest or route changes.</Step>
</Steps>
