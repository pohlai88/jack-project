---
title: 'Authentication and Tenant Access'
description: 'Authentication, tenant selection, optional self-service first organization, tenant login, and invite-token entry points.'
docsType: 'generated-evidence'
---

import { Banner } from 'fumadocs-ui/components/banner';
import { Callout } from 'fumadocs-ui/components/callout';
import { File, Files, Folder } from 'fumadocs-ui/components/files';
import { Step, Steps } from 'fumadocs-ui/components/steps';
import { Tab, Tabs } from 'fumadocs-ui/components/tabs';

<div
  hidden
  data-generated-docs-header="GENERATED FILE - DO NOT EDIT"
  data-source="docs inventory graph"
  data-generated-by="pnpm docs:generate"
/>
<Banner id="feature-auth" changeLayout={false}>
  GENERATED EVIDENCE - This page is rendered from validated product truth. Release state: released.
</Banner>

<Callout title="Truth Surface: Authentication and Tenant Access" type="info">
  - Feature ID: `auth` - Module: `Identity` - Owner: `identity-platform` - Release state: `released`
</Callout>

## Overview: what this feature actually does

Authentication, tenant selection, optional self-service first organization, tenant login, and invite-token entry points.

## When to use this evidence

Use this page when you need the governed feature boundary for Authentication and Tenant Access: owner, release state, routes,
permissions, workflows, APIs, and failure surfaces. Curated pages may explain intent, but this generated page
is the route and contract evidence.

## Source contract

<TypeTable
  type={{
    Module: {
      type: 'Identity',
      description: 'Product module covered by this evidence page.',
      required: true,
    },
    Owner: {
      type: 'identity-platform',
      description: 'Accountable owner for the feature manifest.',
      required: true,
    },
    'Release state': {
      type: 'released',
      description: 'Release lifecycle state declared by product truth.',
      required: true,
    },
    Routes: {
      type: '8 routes',
      description: 'See Route coverage below.',
      required: true,
    },
    Permissions: {
      type: '0 permissions',
      description: 'See Permission coverage below.',
      required: true,
    },
  }}
/>

## Runtime surfaces: routes and permissions

<Tabs items={["Routes", "Permissions"]}>
<Tab>

<Files>
  <Folder name="Routes" defaultOpen>
    <File name="/login" />
    <File name="/select-tenant" />
    <File name="/[locale]/login" />
    <File name="/[locale]/select-tenant" />
    <File name="/[locale]/t/[tenant]/login" />
    <File name="/[locale]/t/[tenant]/invite/[token]" />
    <File name="/[locale]/t/[tenant]/onboarding/cv" />
    <File name="/api/auth/[...nextauth]" />
  </Folder>
</Files>

</Tab>
<Tab>

<Files>
  <Folder name="Permissions" defaultOpen>
    <File name="None declared" />
  </Folder>
</Files>

</Tab>
</Tabs>

## Workflows and actions

<Tabs items={["Workflows", "Actions"]}>
<Tab>

### User sign-in

- ID: `auth.sign-in`
- Summary: Users authenticate and resolve their tenant context.

### Invite acceptance

- ID: `auth.invite-acceptance`
- Summary: Invited users enter a tenant through an invite token.

### Self-service first organization

- ID: `auth.self-service-org`
- Summary: When enabled, users with no memberships can create a tenant and become admin.

</Tab>
<Tab>

### Select tenant

- ID: `auth.select-tenant`
- Summary: Choose an authorized tenant after successful authentication.

### Create organization

- ID: `auth.create-organization`
- Summary: Server action to create a tenant, roles, membership, and person when the feature flag is on.

</Tab>
</Tabs>

## APIs: interface truth

- [`auth.nextauth`](/docs/generated/api/auth.nextauth) - `GET /api/auth/[...nextauth]` - Auth.js route handler for authentication lifecycle requests.

See [OpenAPI Reference](/docs/openapi) for documented request and response interface truth.

## Failure modes: operator risk

- `AFD-AUTH-NO-TENANT` - No tenant membership available: Confirm the user has an active tenant membership or a valid invite.

## Source evidence: traceability

<Steps>
  <Step>Manifest: `src/features/auth/docs.manifest.ts`.</Step>
  <Step>Confirm route, permission, and API references still exist in the product runtime.</Step>
  <Step>Run `pnpm docs:generate` and `pnpm docs:ci` after manifest or route changes.</Step>
</Steps>
