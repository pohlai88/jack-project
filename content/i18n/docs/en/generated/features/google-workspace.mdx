---
title: 'Google Workspace OAuth Integration'
description: 'Google Workspace OAuth connection entry points for tenant integration setup.'
docsType: 'generated-evidence'
---

import { Banner } from 'fumadocs-ui/components/banner';
import { Callout } from 'fumadocs-ui/components/callout';
import { File, Files, Folder } from 'fumadocs-ui/components/files';
import { Step, Steps } from 'fumadocs-ui/components/steps';
import { Tab, Tabs } from 'fumadocs-ui/components/tabs';

{/_
GENERATED FILE - DO NOT EDIT
Source: docs inventory graph
Generated by: pnpm docs:generate
_/}

<Banner id="feature-google-workspace" changeLayout={false}>
  GENERATED EVIDENCE - This page is rendered from validated product truth. Release state: released.
</Banner>

<Callout title="Truth Surface: Google Workspace OAuth Integration" type="info">
  - Feature ID: `google-workspace` - Module: `Integrations` - Owner: `integrations-platform` - Release state: `released`
</Callout>

## Overview: what this feature actually does

Google Workspace OAuth connection entry points for tenant integration setup.

## When to use this evidence

Use this page when you need the governed feature boundary for Google Workspace OAuth Integration: owner, release state, routes,
permissions, workflows, APIs, and failure surfaces. Curated pages may explain intent, but this generated page
is the route and contract evidence.

## Source contract

<TypeTable
  type={{
    Module: {
      type: 'Integrations',
      description: 'Product module covered by this evidence page.',
      required: true,
    },
    Owner: {
      type: 'integrations-platform',
      description: 'Accountable owner for the feature manifest.',
      required: true,
    },
    'Release state': {
      type: 'released',
      description: 'Release lifecycle state declared by product truth.',
      required: true,
    },
    Routes: {
      type: '2 routes',
      description: 'See Route coverage below.',
      required: true,
    },
    Permissions: {
      type: '1 permissions',
      description: 'See Permission coverage below.',
      required: true,
    },
  }}
/>

## Runtime surfaces: routes and permissions

<Tabs items={["Routes", "Permissions"]}>
<Tab>

<Files>
  <Folder name="Routes" defaultOpen>
    <File name="/api/integrations/google-workspace/callback" />
    <File name="/api/integrations/google-workspace/connect" />
  </Folder>
</Files>

</Tab>
<Tab>

<Files>
  <Folder name="Permissions" defaultOpen>
    <File name="admin:integrations" />
  </Folder>
</Files>

</Tab>
</Tabs>

## Workflows and actions

<Tabs items={["Workflows", "Actions"]}>
<Tab>

### Google Workspace OAuth connection

- ID: `google-workspace.oauth-connect`
- Summary: Start and complete Google Workspace OAuth for tenant integration setup.

</Tab>
<Tab>

### Connect Google Workspace account

- ID: `google-workspace.connect`
- Summary: Generate Google OAuth state and redirect to Google authorization.

</Tab>
</Tabs>

## APIs: interface truth

- [`google-workspace.oauth.callback`](/docs/generated/api/google-workspace.oauth.callback) - `GET /api/integrations/google-workspace/callback` - Handle Google Workspace OAuth callback.
- [`google-workspace.oauth.connect`](/docs/generated/api/google-workspace.oauth.connect) - `GET /api/integrations/google-workspace/connect` - Start Google Workspace OAuth authorization.

See [OpenAPI Reference](/docs/openapi) for documented request and response interface truth.

## Failure modes: operator risk

- `AFD-GOOGLE-WORKSPACE-OAUTH` - Google Workspace OAuth failed: Validate OAuth state, Google credentials, callback URL, and tenant context.

## Source evidence: traceability

<Steps>
  <Step>Manifest: `src/features/google-workspace/docs.manifest.ts`.</Step>
  <Step>Confirm route, permission, and API references still exist in the product runtime.</Step>
  <Step>Run `pnpm docs:generate` and `pnpm docs:ci` after manifest or route changes.</Step>
</Steps>
