# Landing Archive

This folder keeps retired landing-page source outside the App Router tree.
Nothing here is imported by the active codebase — safe to delete once no longer needed for reference.

## Contents

- `legacy-locale-page.tsx` — former `src/app/[locale]/page.tsx` implementation from before the marketing landing promotion.
- `_landing-components/` — all pre-v2 landing section and UI components formerly at `src/app/_landing-components`. Archived when the marketing route group became the canonical landing surface.

## Active canonical entry

`src/app/[locale]/(marketing)/page.tsx` — the canonical marketing landing page.
